# Server
PORT=8080
HOST=0.0.0.0

# Postgres (local or Neon)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vehicle_home
PGSSL=0         # set to 1 for Neon/Supabase/etc (TLS)

# JWT secret
JWT_SECRET=change-me
